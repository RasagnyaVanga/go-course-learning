# Generics 
* Making a custom type queue using generics so that it can hold any type of data.
* type Queue[T any] struct - defines a queue that holds elements of type T.
* Enqueue function on queue - adds an element to back of the queue.
* Dequeue function on queue - removes and returns front element. 
* Peek function - returns the front element. 