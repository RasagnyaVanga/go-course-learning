Downloaded the go tar into the system.
Extracted these files into /usr/local.
Now i got the go files into /usr/local. i.e., /usr/local/go
now we need to export the binary file path of go in .zshrc file
reload the .zshrc file using source
so that we can access go from anywhere. 